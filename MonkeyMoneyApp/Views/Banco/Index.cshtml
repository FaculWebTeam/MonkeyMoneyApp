@model IEnumerable<ApiMonkeyMoney.Models.Banco>

@{
    ViewBag.Title = "Lista de Bancos";
    Layout = "~/Views/Shared/_LayoutWithSidebar.cshtml";
}

<h2 class="text-center mb-4">Lista de Bancos</h2>

<div class="container">
    <form method="get" action="/Banco/GetByName" class="form-inline mb-4">
        <div class="input-group">
            <input type="text" name="name" class="form-control" placeholder="Pesquisar Banco" />
            <button type="submit" class="btn btn-primary">Pesquisar</button>
        </div>
    </form>
    @if (!string.IsNullOrEmpty(ViewBag.Mensagem as string))
    {
        <div class="alert alert-warning" role="alert">
            @ViewBag.Mensagem
        </div>
    }
    <div class="table-responsive">
        <table class="table table-bordered table-hover">
            <thead class="thead-light">
                <tr>
                    <th scope="col">ID</th>
                    <th scope="col">Nome</th>
                    <th scope="col">Ações</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var banco in Model)
                {
                    <tr>
                        <td>@banco.Id</td>
                        <td>@banco.Nome</td>
                        <td>
                            <a href="@Url.Action("Edit", "Banco", new { id = banco.Id })" class="btn btn-warning btn-sm">Editar</a>
                            <a href="@Url.Action("Delete", "Banco", new { id = banco.Id })" class="btn btn-danger btn-sm">Excluir</a>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>
